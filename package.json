{
  "name": "@nwo/stan",
  "version": "0.0.0",
  "description": "an agent that stays on standby",
  "main": "index.js",
  "bin": {
    "stan": "bin/stan"
  },
  "scripts": {
    "preversion": "npm test",
    "version": "npm publish --access public",
    "postversion": "git push && git push --tags --no-verify",
    "test": "echo \"Error: no test specified\""
  },
  "author": "Brock Whitten <brock@sintaxi.com>",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
